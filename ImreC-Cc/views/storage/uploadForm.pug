p
    span #[b Note]: you can add more then file at once#[br]
    span #[b Note]: drag&drop also works#[br]
    span #[b Note]: info about success upload hides automatically#[br]
    span #[b Note]: info about failed upload can be hidden by clicking on red message#[br]

p
    span(class="upload-queue-info")="Files in upload queue: "
    span(id="upload-queue" class="upload-queue-info")="0"

form(action=s3form.url method="post" enctype="multipart/form-data" id="s3form")
    each key in s3form.s3fields
        input(type="hidden" name=key.name value=key.value)

    span(class="btn btn-info btn-file") just #[b ADD] some new #[b FILES]
        input(type="file" name="file" id="file" required="required" multiple="multiple")

div(id="progress-bar-area" class="progress-bar-area padding-top")
